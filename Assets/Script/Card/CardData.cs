public enum CardDataType : byte
{
    Attack = 0,
    Shield = 1,
    Skill = 2
}

public enum AttackCardData : byte
{
    Attack = 0
}

public enum ShieldCardData : byte
{
    Shield = 0
}

public enum SkillData : byte
{
    Hill = 0
}
